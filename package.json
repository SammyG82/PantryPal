{
  "scripts": {
    "dev": "concurrently \"cd backend && uvicorn app:app --reload --port 8000\" \"npm run dev --prefix frontend\"",
    "dev:backend": "cd backend && uvicorn app:app --reload --port 8000",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
